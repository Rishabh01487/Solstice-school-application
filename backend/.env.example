# ── Database ──
DATABASE_URL=postgresql+asyncpg://edunexus:edunexus_pass@db:5432/edunexus
DATABASE_URL_SYNC=postgresql://edunexus:edunexus_pass@db:5432/edunexus

# ── Redis ──
REDIS_URL=redis://redis:6379/0

# ── JWT ──
SECRET_KEY=change-me-to-a-random-64-char-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── App ──
APP_NAME=EduNexus School
APP_ENV=development
DEBUG=true
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ── File Storage ──
STORAGE_BACKEND=minio
GCS_BUCKET=edunexus-files
GCS_CREDENTIALS_PATH=

# MinIO (local dev)
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=edunexus-files
MINIO_USE_SSL=false

# ── Email (placeholder) ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
